version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: irbistheatre_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: ADMIN
      POSTGRES_DB: IrbisTheatre
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: